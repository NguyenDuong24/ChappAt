# Environment Variables for Coin Server

# Server Configuration
NODE_ENV=development
PORT=3000

# CORS Configuration (comma-separated URLs for production)
# Example: ALLOWED_ORIGINS=https://yourapp.com,https://www.yourapp.com
ALLOWED_ORIGINS=*

# Firebase Admin SDK
# You need to download firebase-service-account.json from Firebase Console
# Place it in the coin-server directory
# Instructions:
# 1. Go to Firebase Console > Project Settings > Service Accounts
# 2. Click "Generate New Private Key"
# 3. Save as firebase-service-account.json in coin-server folder
