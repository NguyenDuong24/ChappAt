# 🚫 BLOCK USER SYSTEM - VISUAL GUIDE

```
┌─────────────────────────────────────────────────────────────────┐
│                    BLOCK USER FLOW                               │
└─────────────────────────────────────────────────────────────────┘

┌──────────────┐
│ User Profile │
└──────┬───────┘
       │
       ├─► 👤 View Profile
       │   ├─► Follow Button
       │   └─► Block Menu (...)
       │       ├─► Tap menu
       │       ├─► Show "Chặn"/"Bỏ chặn"
       │       └─► Confirm dialog
       │           ├─► Cancel
       │           └─► Block ✅
       │
       ├─► 🚫 After Block:
       │   ├─► Auto unfollow (both ways)
       │   ├─► Show "Đã chặn" banner
       │   └─► Save to Firestore
       │
       └─► 📱 Content Hidden:
           ├─► Posts ❌
           ├─► Comments ❌
           ├─► Messages ❌
           └─► Profile limited

┌─────────────────────────────────────────────────────────────────┐
│                    SYSTEM ARCHITECTURE                           │
└─────────────────────────────────────────────────────────────────┘

┌──────────────┐
│  UI Layer    │ ← TopProfileUserProfileScreen
└──────┬───────┘   ← BlockedContentWrapper
       │           ← PostFeed, CommentList, etc.
       ↓
┌──────────────┐
│  Hooks       │ ← useBlockStatus()
└──────┬───────┘   ← useFilterBlockedContent()
       │
       ↓
┌──────────────┐
│  Service     │ ← followService.blockUser()
└──────┬───────┘   ← followService.unblockUser()
       │           ← followService.isBlocked()
       ↓
┌──────────────┐
│  Database    │ ← Firestore: blocks/
└──────────────┘   ← { blockerId, blockedId, createdAt }

┌─────────────────────────────────────────────────────────────────┐
│                    USAGE PATTERNS                                │
└─────────────────────────────────────────────────────────────────┘

Pattern 1: FILTER LIST (Recommended for feeds)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
const { filteredItems } = useFilterBlockedContent(posts, userId);

┌─────────┐     ┌──────────┐     ┌─────────┐
│ Post 1  │     │ Post 2   │     │ Post 3  │
│ user123 │ ──► │ user456  │ ──► │ user789 │
│   ✅    │     │   🚫     │     │   ✅    │
└─────────┘     └──────────┘     └─────────┘
   Show          Blocked           Show


Pattern 2: WRAP INDIVIDUAL (Good for single items)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
<BlockedContentWrapper targetUserId={post.userID}>
  <PostCard />
</BlockedContentWrapper>

┌────────────────────┐
│ BlockedContent     │
│ Wrapper            │
│  ┌──────────────┐  │     ┌──────────────┐
│  │ Post Content │  │ ──► │ 🚫 Ẩn nội    │
│  │ user456      │  │     │    dung      │
│  └──────────────┘  │     └──────────────┘
└────────────────────┘


Pattern 3: CHECK STATUS (For conditional rendering)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
const { isBlocked, isBlockedBy } = useBlockStatus(userId, targetId);

if (isBlocked) return <BlockedView />;
if (isBlockedBy) return <CannotAccessView />;

┌─────────────┐
│ Check Block │
│   Status    │
└──────┬──────┘
       │
       ├─► isBlocked = true     ──► Show: "Bạn đã chặn"
       ├─► isBlockedBy = true   ──► Show: "Bị chặn"
       └─► no block             ──► Show: Normal content

┌─────────────────────────────────────────────────────────────────┐
│                    UI COMPONENTS                                 │
└─────────────────────────────────────────────────────────────────┘

Profile Screen:
┌────────────────────────────────────────────────┐
│  👤 Avatar          [Theo dõi] [...]  ← Menu   │
│                                                 │
│  🚫 Bạn đã chặn người dùng này  ← Banner       │
│                                                 │
│  ℹ️ User Info                                   │
└────────────────────────────────────────────────┘

Block Menu:
┌──────────────────┐
│ [...]  ← Click   │
└───┬──────────────┘
    │
    ↓
┌──────────────────┐
│ 🔒 Chặn user     │ ← Red color
└──────────────────┘
    │
    ↓
┌────────────────────────────────┐
│ ⚠️  Chặn người dùng?            │
│                                 │
│ • Không thấy bài viết          │
│ • Không nhắn tin được          │
│ • Tự động unfollow             │
│                                 │
│  [Hủy]         [Chặn] ← Red    │
└────────────────────────────────┘

Blocked Content Placeholder:
┌────────────────────────────────┐
│  👁️‍🗨️  Nội dung đã bị ẩn         │
└────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    FIRESTORE STRUCTURE                           │
└─────────────────────────────────────────────────────────────────┘

blocks (collection)
  └─ {blockId} (document)
      ├─ blockerId: "user123"     ← Người chặn
      ├─ blockedId: "user456"     ← Người bị chặn
      └─ createdAt: timestamp     ← Thời gian

Example queries:
• Get all users blocked by user123:
  where('blockerId', '==', 'user123')

• Check if user123 blocked user456:
  where('blockerId', '==', 'user123')
  where('blockedId', '==', 'user456')

┌─────────────────────────────────────────────────────────────────┐
│                    CODE SNIPPETS                                 │
└─────────────────────────────────────────────────────────────────┘

1️⃣ Block a user:
━━━━━━━━━━━━━━━
await followService.blockUser(currentUserId, targetUserId);


2️⃣ Unblock a user:
━━━━━━━━━━━━━━━━
await followService.unblockUser(currentUserId, targetUserId);


3️⃣ Check if blocked:
━━━━━━━━━━━━━━━━━
const isBlocked = await followService.isBlocked(userId1, userId2);


4️⃣ Filter posts:
━━━━━━━━━━━━━━━
const { filteredItems } = useFilterBlockedContent(posts, userId);


5️⃣ Wrap content:
━━━━━━━━━━━━━━━
<BlockedContentWrapper targetUserId={userId}>
  <Content />
</BlockedContentWrapper>


6️⃣ Check status:
━━━━━━━━━━━━━━━
const { isBlocked, isBlockedBy } = useBlockStatus(userId1, userId2);

┌─────────────────────────────────────────────────────────────────┐
│                    TESTING CHECKLIST                             │
└─────────────────────────────────────────────────────────────────┘

✅ Profile Screen:
   ├─ [ ] Menu button hiển thị
   ├─ [ ] Click menu → show "Chặn"
   ├─ [ ] Click Chặn → confirmation
   ├─ [ ] Confirm → chặn thành công
   └─ [ ] Banner "Đã chặn" hiển thị

✅ Post Feed:
   ├─ [ ] Posts của user bị chặn biến mất
   ├─ [ ] Hoặc show placeholder "Đã ẩn"
   └─ [ ] Posts của user khác vẫn hiện

✅ Comments:
   ├─ [ ] Comments của user bị chặn ẩn
   └─ [ ] Comments khác vẫn hiện

✅ Chat:
   ├─ [ ] Không thể nhắn tin
   └─ [ ] Show message "Không thể chat"

✅ Unblock:
   ├─ [ ] Click "Bỏ chặn"
   ├─ [ ] Không cần confirm
   └─ [ ] Content hiện lại

✅ Database:
   ├─ [ ] Record tạo trong blocks/
   ├─ [ ] blockerId đúng
   ├─ [ ] blockedId đúng
   └─ [ ] timestamp có

┌─────────────────────────────────────────────────────────────────┐
│                    TROUBLESHOOTING                               │
└─────────────────────────────────────────────────────────────────┘

❌ Problem: Content vẫn hiển thị sau khi chặn
✅ Solution: 
   • Check field name: userID vs userId
   • Verify currentUser.uid được truyền
   • Check console log

❌ Problem: Loading quá lâu
✅ Solution:
   • Dùng useFilterBlockedContent thay vì wrap từng item
   • Implement pagination
   • Cache at higher level

❌ Problem: Menu không hiện
✅ Solution:
   • Check showBlockMenu state
   • Verify currentUser !== targetUser
   • Check z-index của menu

❌ Problem: Không chặn được
✅ Solution:
   • Check Firebase permissions
   • Verify followService import
   • Check error in console

┌─────────────────────────────────────────────────────────────────┐
│                    PERFORMANCE TIPS                              │
└─────────────────────────────────────────────────────────────────┘

🚀 Use useFilterBlockedContent for lists (1 query for all items)
🚀 Use BlockedContentWrapper for single items
🚀 Cache block relationships at app level
🚀 Lazy load - only check when needed
🚀 Batch queries when possible

┌─────────────────────────────────────────────────────────────────┐
│                    QUICK REFERENCE                               │
└─────────────────────────────────────────────────────────────────┘

Files Created:
✅ hooks/useBlockStatus.ts
✅ components/common/BlockedContentWrapper.tsx
✅ components/examples/BlockedContentExamples.tsx
✅ BLOCK_USER_GUIDE.md
✅ BLOCK_QUICK_START.tsx
✅ BLOCK_SYSTEM_COMPLETE.md

Files Modified:
✅ services/followService.ts (added block methods)
✅ components/profile/TopProfileUserProfileScreen.tsx (full UI)

Main Imports:
import { useBlockStatus, useFilterBlockedContent } from '@/hooks/useBlockStatus';
import { BlockedContentWrapper } from '@/components/common/BlockedContentWrapper';
import { followService } from '@/services/followService';

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎉 BLOCK SYSTEM COMPLETE - READY TO USE! 🎉
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
